# TODO List R2D2 - Sovereign Shield (r2d2-jsonai)

**Date :** 2026-04-09
**Cible :** `r2d2-jsonai/src/lib.rs`

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Fusion Sémantique Structurale (Edge)**
  - Supprimer les champs isolés `pub ontological_tags: Vec<OntologyRel>` et `pub dependencies: Vec<String>`.
  - Créer la structure de liaison : `pub struct Edge { pub rel: OntologyRel, pub target_id: String }`.
  - Remplacer par `pub edges: Vec<Edge>` dans `JsonAiV3`.
- `[ ]` **Tâche 2 : Nettoyage d'État (Zero-Bloat)**
  - Supprimer le champ complet `pub is_fact: bool` de `JsonAiV3` (afin d'éviter la sérialisation inutile et les conflits logiques). La méthode d'accès doit dynamiquement déduire cette vérité de `belief_state`.
- `[ ]` **Tâche 3 : Génération Dense (Dataloader Anti-bruit)**
  - Implémenter le trait `std::fmt::Display` (et/ou via une méthode spécifique `to_dense_embedding() -> String`) pour `JsonAiV3`.
  - Cette méthode devra formatter la chaine en un format dense dépourvu des guillemets et accolades du JSON pour préserver l'hyper-attention des Gated-LMs, tel que : `<[Belief]|[Consensus]> le texte brut pertinent <[REL]:id>`.
