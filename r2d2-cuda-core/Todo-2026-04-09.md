# TODO List R2D2 - Sovereign Shield (r2d2-cuda-core)

**Date :** 2026-04-09
**Cible :** `r2d2-cuda-core/src/lib.rs` (et `Cargo.toml`)

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Dynamic Loading Cudarc**
  - Modifier le `Cargo.toml` de la crate pour inclure la dépendance `cudarc` avec les flags `["dynamic-loading", "cuda-version-from-build-system", "fallback-latest"]`. Ceci empêchera le Linker Host de causer des instabilités au démarrage.
- `[ ]` **Tâche 2 : JIT Compilation Pipeline (PTX)**
  - Instaurer un mécanisme asynchrone chargeant dynamiquement le BitNet `.ptx` au moyen de la méthode `nvrtc::compile_ptx()` ou `context.load_module()`. Supprimer la FFI statique pure.
- `[ ]` **Tâche 3 : RAII sur VRAM (Ownership Rust)**
  - Remplacer TOUS les arguments de type pointeur brut C (`*const u8`, `*mut f32`) par les primitives de possession natives de cudarc : `driver::CudaSlice<T>` et `driver::CudaView<T>`. Confier le cycle de la RAM au Borrow Checker.
- `[ ]` **Tâche 4 : Allocation VRAM non Bloquante**
  - Coder spécifiquement l'utilisation de `cudaMallocAsync` pour remplacer les Mallocs natifs lors des réservations, assurant un flot continu pour l'optimisation des flux asynchrones Bitmamba.
- `[ ]` **Tâche 5 : Safe Execution (Zero-Panic)**
  - Envelopper les appels `LaunchArgs::launch()` de cudarc et intercepter les Result `cudaError_t` pour qu'à aucun instant une erreur Cuda ne panique et corrompe le thread parent Rust.
