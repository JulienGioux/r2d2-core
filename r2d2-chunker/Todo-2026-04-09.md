# TODO List R2D2 - Sovereign Shield (r2d2-chunker)

**Date :** 2026-04-09
**Cible :** `r2d2-chunker/src/text_chunker.rs`

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Couplage Token-Aware Obligatoire**
  - Câbler la fonction `chunk_text()` pour prendre explicitement l'instance ou la fonction d'encodage BPE (`r2d2-tokenizer`).
  - Supprimer totalement la primitive `max_chars` pour basculer sur un strict `max_tokens` (ex: `ctx_window = 512`).
- `[ ]` **Tâche 2 : Encodeur Incrémental (Linear Time O(N))**
  - Mettre en place un système de comptage incrémental : au lieu de ré-encoder chaque concaténation de mots de bout en bout pour savoir si la fenêtre explose, le Chunker doit utiliser un état `AppendableEncoder` ou une approximation mémorisée.
- `[ ]` **Tâche 3 : Frontière Sémantique Stricte**
  - Plutôt que d'itérer sur des espaces `split_whitespace()`, le flux doit être décomposé en paragraphes `\n\n`, puis en phrases. 
  - On cumule les Tokens BPE block par block. Uniquement si un paragraphe massif dépasse `max_tokens`, on bascule sur la découpe granulaire à la phrase/virgule.
- `[ ]` **Tâche 4 : Fenêtre Glissante Paramétrable `Token-Overlap`**
  - Remplacer le concept d'overlapping abstrait par une variable pure `overlap_tokens` calculée depuis la fin de l'itération précédente pour ne jamais briser l'attention contextuelle d'un tenseur LLM lors de sa consommation.
