# TODO List R2D2 - Sovereign Shield (r2d2-secure-mem)

**Date :** 2026-04-09
**Cible :** `r2d2-secure-mem/src/lib.rs`

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Remplacer le `#[derive(Debug)]` automatique**
  - Supprimer le `#[derive(Debug)]` de la structure `SecureMemGuard`.
  - Implémenter le trait `std::fmt::Debug` manuellement pour forcer l'affichage opaque (Ex: `f.write_str("SecureMemGuard(<REDACTED>)")`).
- `[ ]` **Tâche 2 : Interdiction Algorithmique du Clonage**
  - Retirer le trait `Clone` de la contrainte générique de `SecureMemGuard<T>`. Le type doit devenir "Move-only" (Affine Type).
- `[ ]` **Tâche 3 : Enforcer le `Zeroizing` à la source**
  - Modifier le constructeur `new(data: T)` pour exiger `data: zeroize::Zeroizing<T>` afin de garantir que l'appelant a passé une donnée qui sera effacée de la pile, évitant ainsi le fantôme de copie ("Stack Leak") de `Box::new()`.
- `[ ]` **Tâche 4 : Implémenter le "Typestate Pattern" (Active/Revoked)**
  - Retirer l'erreur dynamique `RevokedAccess` de l'`enum SecureMemError`.
  - Ajouter un marqueur de Typestate `<T, State>` (`pub struct Active;`, `pub struct Revoked;`).
  - Lier `expose_payload()` strictement à `SecureMemGuard<T, Active>`.
  - Ajouter une méthode de transition `revoke(self) -> SecureMemGuard<T, Revoked>`.
