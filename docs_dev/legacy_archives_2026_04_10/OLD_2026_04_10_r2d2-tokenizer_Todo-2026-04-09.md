# TODO List R2D2 - Sovereign Shield (r2d2-tokenizer)

**Date :** 2026-04-09
**Cible :** `r2d2-tokenizer/src/`

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Remplacement par Aho-Corasick**
  - Supprimer les Regex de pré-tokenisation.
  - Implémenter l'automate purement linéaire `aho-corasick` pour éviter toute faille applicative ReDoS (Time-of-service attack) lors des entrées textuelles.
- `[ ]` **Tâche 2 : Transition vers une structure O(1)**
  - Coder la structure "Linked Array" (Liste doublement chaînée avec un ID d'arène, typique `Vec<Node>`) pour effectuer les fusions BPE sans recopie de mémoire constante.
- `[ ]` **Tâche 3 : Court-Circuit BPE (Trie Bypass)**
  - Intégrer un structure de type Dictionnaire-Trie. Si un mot textuel soumis complète le filtre `Trie`, assigner le token id final et `continue`, évitant la boucle combinatoire chronophage des paires BPE de HuggingFace.
- `[ ]` **Tâche 4 : Parallélisation M-Batch**
  - Importer `rayon` pour que le Datapipeline puisse traiter simultanément le Chunking des lignes JSON par batch, permettant une sollicitation 100% de tous les cœurs CPU disponibles sur de larges datasets.
