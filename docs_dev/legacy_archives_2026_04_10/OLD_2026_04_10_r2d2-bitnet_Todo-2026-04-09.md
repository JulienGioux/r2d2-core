# TODO List R2D2 - Sovereign Shield (r2d2-bitnet)

**Date :** 2026-04-09
**Cible :** `r2d2-bitnet/` (notamment `custom_op_cuda.rs` et `moe.rs`)

## Tâches d'Architecture Sécuritaire :
- `[x]` **Tâche 1 : Expulsion des pointeurs nus (*const u8)**
  - Changer TOUTE l'API `custom_op_cuda.rs` pour qu'elle accepte uniquement des `driver::CudaSlice<T>` ou des vues `driver::CudaView<T>` afin de garantir l'absence d'out-of-bounds sur le GPU via les garanties du Borrow Checker Rust.
- `[x]` **Tâche 2 : Architecture "Dynamic Loading" (Adieu rust-lld C++)**
  - Modifier le Cargo/Build pour générer du PTX brut (Texte) hors-ligne via `nvcc --ptx -arch=native`. Éradiquer le système de `#[link]` classique provoquant les crashs avec les éditeurs de liens Hôtes (ex: GCC/rust-lld sur Fedora). Chargement asynchrone garanti via `get_or_load_custom_func`.
- `[x]` **Tâche 3 : `cudaMallocAsync` & VRAM Memory Pools**
  - Remplacer les allocations bloquantes au niveau du `SparseMoE` par des allocateurs Asynchrones rattachés directement au Cuda Stream du Model Pass, empêchant la fragmentation mémoire et les engorgements de la Queue Device.
- `[ ]` **Tâche 4 : Objectif Long-Terme (`CubeCL`)**
  - Remplacer les appels complexes CUDA Custom (comme la `FWHT` et le Routing Vectoriel) par du pur Rust compilé JIT via `CubeCL`, autorisant l'Auto-Tuning matériel au runtime (optimisations des warps, TMA) sans code C++ lié.
