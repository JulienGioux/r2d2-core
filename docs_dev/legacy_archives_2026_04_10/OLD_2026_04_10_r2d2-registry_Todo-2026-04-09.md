# TODO List R2D2 - Sovereign Shield (r2d2-registry)

**Date :** 2026-04-09
**Cible :** `r2d2-registry/src/manager.rs`

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Brider la Profondeur de parsing TOML**
  - Remplacer `toml::from_str` par la désérialisation manuelle via `toml::Deserializer` configurée pour limiter la profondeur et prévenir le Stack Exhaustion effect (Crash).
- `[ ]` **Tâche 2 : Élimination du vecteur TOCTOU (Symlink)**
  - Utiliser la librairie `rustix` ou `libc` pour utiliser des descripteurs de répertoires absolus (`O_PATH` | `O_DIRECTORY`) couplés avec l'appel système `openat()`. Cela garantira mathématiquement qu'aucun symlink n'a remplacé le dossier.
- `[ ]` **Tâche 3 : Verrouillage Posix (Rust 1.89+)**
  - Ajouter les méthodes `File::try_lock_shared` et `File::try_lock_exclusive` sur le registre lors de la lecture des méta-données pour contrer les accès asynchrones du FileSystem de l'OS. Mettre en place des retry loops.
- `[ ]` **Tâche 4 : Implémentation du pattern "Loader Thread" (Anti-Starvation I/O)**
  - Ne plus charger les fichiers via `.read_to_string()` de manière impérative. Passer sur un modèle asynchrone (Spawn `tokio::task::spawn_blocking` comme en Phase 9 pour le MCP) avec l'Orchestrateur pour séparer le pipeline de Lecture/Disk du Thread global ou de l'Inférence.
