# ROADMAP & TODO - 10 Avril 2026

## Tâches Immédiates & Dettes Techniques

- [ ] **The Chimera Engine (JsonAI v3.1)** : Refonte chirurgicale de la redondance `is_fact` / `belief_state`. 
  - *Contexte* : On a constaté que la structure `JsonAiV3` utilise un booléen `is_fact` couplé à un Enum `BeliefState`. C'est dangereux et non-Hexagonal (État invalide possible).
  - *Action requise* : Éliminer `is_fact` sur plus de 24 occurrences (Blackboard DB SQL, Serveur MCP, UI, Tokenizer) et basculer sur un test typé strict du tuple ou de la structure.
- [ ] **Sovereign R2D2-UI** : Finaliser l'intégration AuthZ pour sceller le HTMX. L'UI est bloquée à 80%.
- [ ] **R2D2-Paradox** : Implémenter les "Plugins d'Ontologie Stricts" pour la validation des circuits lents.

## Tâches Validées Aujourd'hui
- [x] **R2D2-Vampire (MCP)** : Sécurisation du `deploy_to_antigravity.sh` avec paths dynamiques complets.
- [x] **R2D2-Vampire (MCP)** : Pont CDP Windows-WSL finalisé (`r2d2-bridge`).
- [x] **Workflow global** : Implémentation du système C.A.D dans `verify-last-changes.md`.
- [x] **Protocole Temp-Réel** : Création de `.cursorrules` pour automatiser la log système de l'IA (mémoire temps-réel des échanges).
- [x] **Garde-fou IA** : Implémentation du périmètre de Ligne Rouge dans `.cursorrules` pour interdire la modification hors Implementation Plan.
- [x] **Sovereign Browser (UX Distribution)** : Implémentation de l'Auto-Ignition (WSL interop) locale depuis Antigravity et `Drop` RAII pour le nettoyage du processus fantôme.
- [x] **Système (Heuristique WSL/Win)** : Implémentation de l'exploration dynamique multi-chemins pour PowerShell (`pwsh.exe` vs `powershell.exe`) dans les scripts Bash et Rust, évitant l'échec de distribution.
