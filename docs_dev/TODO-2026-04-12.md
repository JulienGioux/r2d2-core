# TODO du 12 Avril 2026 (Continuous Architecture)

## Dette Technique Consciemment Contractée & Features Reportées
1. **Bibliothèque `notebooklm-rs` (Phase 2 repoussée)** :
   - FFI / Bindings C : La génération du `notebooklm.h` via `cbindgen` et l'exposition stricte en blocs `extern "C"` avec `catch_unwind` a été reportée pour garantir un risque zéro (Zero Dette Technique). Cela devra être implémenté lors d'une étape `V2` dédiée lorsque la crate `notebooklm-core` sera pleinement testée et certifiée ISO.
   - Script `diagnose_get_notebook` / `check_rpc_health` : Remplacement du script python abandonné temporairement.

## Tâches Résolues
1. **Implémentation de `notebooklm-rs` (Dossier libs)** :
   - **Isolation Hexagonale** terminée avec `notebooklm-core` (Safe Rust).
   - Intégration de l'acteur **ChromiumActor** pour empêcher les locks concurrentiels (Thread Starvation) avec un navigateur partagé.

## Vulnérabilités ou Failles Identifiées (à combler prochainement)
1. Parsing RPC complexe (Liste Artefacts) à affiner dans l'Adaptateur Client pour extraire parfaitement tous les IDs profonds (actuellement un mock ou implémentation basique partielle dans `list_artifacts`). Il faudra parser correctement l'index Google pour capturer les status de chaque élément.
