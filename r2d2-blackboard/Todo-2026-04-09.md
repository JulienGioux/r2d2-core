# TODO List R2D2 - Sovereign Shield (r2d2-blackboard)

**Date :** 2026-04-09
**Cible :** `r2d2-blackboard/src/lib.rs`

## Tâches d'Architecture Sécuritaire :
- `[ ]` **Tâche 1 : Expulsion du DDL (`CREATE TABLE`) au Shell**
  - Retirer intégralement la méthode `initialize_registry_tables()` et la création conditionnelle cachée dans `save_reflex()`.
  - Migrer cette initialisation vers un script bash solide `.sql` exécuté uniquement au "Boot" du système et tracé strictement.
- `[ ]` **Tâche 2 : Thread d'Isolation DB (`Worker-Port`)**
  - Modifier le type de la base de données. Soit `sqlx` est instancié sur son *propre* micro-runtime Tokio encapsulé dans un `std::thread`, lié au reste du soft via `mpsc::sync_channel` ; soit la crate doit basculer vers PostgreSQL Synchrone (`rust-postgres`, ou `diesel`).
- `[ ]` **Tâche 3 : Connexion "Eager" & Résilience Anti-DDoS**
  - Forcer la pré-allocation et la vérification des `20` connexions au démarrage, interdiction du mode `Lazy Connect` pour valider le "Fail-Fast".
  - Refondre la Retry Loop de fonction `anchor_fragment()`. Le `tokio::time::sleep(delay)` doit incorporer un *Jitter* (ex: Randomisé de `± 100ms` à chaque rebond).
- `[ ]` **Tâche 4 : Purge du `REINDEX CONCURRENTLY`**
  - Expulser la méthode `compress_vector_index()` hors de cette crate applicative de run-loop. Son enveloppe devra être appelée par `r2d2-circadian` (Daemon de nuit).
