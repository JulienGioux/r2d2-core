---
description: Ask_RustyMaster (Legacy Alias -> Utiliser /ask-rustymaster)
---

**Note Historique :** Ce prompt global a été déprécié au profit d'invocations plus précises. 

**Si ce workflow est appelé :**
Interagis avec `RustyMaster` via `ask_consultant` en lui présentant uniquement l'intention globale de l'utilisateur (le "Big Picture") et tes plans techniques généraux avant de toucher au code de la Forteresse R2D2. 

**Protocole :**
1. Comprends la feature ou le besoin utilisateur.
2. Interroge `RustyMaster` une SEULE fois en début de tâche, en incluant un résumé condensé du contexte et des fichiers cibles.
3. Ne boucle pas à chaque petite portion de code. L'architecte donne la direction, tu implémentes l'ingénierie tactique.  
4. Une fois l'implémentation complètement finalisée et testée, tu peux (optionnellement) générer un bref résumé pour révision si c'est critique, sinon termine la tâche.
